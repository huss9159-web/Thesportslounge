SportsLounge2 - Vercel + MongoDB Ready

Instructions:
1. Unzip project.
2. Install deps: npm install
3. Set environment variable in Vercel dashboard: MONGODB_URI = your_mongo_connection_string
4. Deploy with: vercel (or push to Git and connect to Vercel)

Notes:
- This version keeps your original frontend (index.html + script.js).
- Backend endpoints mirror the original SQLite server behavior, using MongoDB.
# Thesportslounge
